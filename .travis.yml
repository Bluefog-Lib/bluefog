language: python
python:
    - "3.7"

os: linux

addons:
    apt:
      update: true
    homebrew:
      update: true

before_install:
    - ./scripts/install_mpi.sh openmpi

install:
    pip install .

script:
    - make test
